<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<meta name="description" content="Get your free professional credit audit from 700 Credit Club Experts. Upload your credit report and receive a full AI-powered analysis — negative items, utilization, inquiries, and a step-by-step repair plan. Powered by JECI AI."/>
<meta name="keywords" content="free credit audit, credit repair, credit score improvement, 700 credit club, credit analysis, JECI AI"/>
<meta property="og:title" content="Free Credit Audit — 700 Credit Club Experts"/>
<meta property="og:description" content="Upload your credit report and get a full professional credit audit in seconds. Free, no hard pull, privacy protected."/>
<meta property="og:type" content="website"/>
<meta property="og:url" content="https://www.700creditclubexperts.com/free-credit-audit"/>
<title>Free Credit Audit | 700 Credit Club Experts — Powered by JECI AI</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,600;0,700;1,400&family=DM+Sans:opsz,wght@9..40,300;9..40,400;9..40,500;9..40,600&display=swap"/>

<style>
/* ── Reset & Base ─────────────────────────────────────── */
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
html{scroll-behavior:smooth;}
body{
  font-family:'DM Sans',sans-serif;
  color:#1A1A2E;
  background:#F5F3EE;
  min-height:100vh;
  -webkit-font-smoothing:antialiased;
}

/* ── Brand Tokens ─────────────────────────────────────── */
:root{
  --navy:#0C1F3F;
  --navy-dark:#070F1E;
  --navy-light:#1A3260;
  --gold:#C9A84C;
  --gold-light:#E8C97A;
  --gold-dark:#A88830;
  --off-white:#F5F3EE;
  --white:#FFFFFF;
  --text:#1A1A2E;
  --muted:#6B7A99;
  --border:rgba(0,0,0,0.08);
  --radius-sm:6px;
  --radius-md:10px;
  --radius-lg:14px;
  --radius-xl:18px;
  --shadow-card:0 2px 20px rgba(0,0,0,0.07);
  --shadow-lift:0 8px 40px rgba(0,0,0,0.10);
}

/* ── Typography ───────────────────────────────────────── */
.serif{font-family:'Cormorant Garamond',serif;}
h1,h2,h3{font-family:'Cormorant Garamond',serif;line-height:1.1;}

/* ── Animations ───────────────────────────────────────── */
@keyframes spin{to{transform:rotate(360deg);}}
@keyframes fadeUp{from{opacity:0;transform:translateY(22px);}to{opacity:1;transform:translateY(0);}}
@keyframes pulse{0%,100%{opacity:1;}50%{opacity:0.35;}}
@keyframes shimmer{0%{opacity:0.6;}50%{opacity:1;}100%{opacity:0.6;}}
.fade-up{animation:fadeUp 0.65s cubic-bezier(.22,.68,0,1.2) forwards;}

/* ── Buttons ──────────────────────────────────────────── */
.btn-gold{
  display:inline-flex;align-items:center;gap:8px;
  background:var(--gold);color:var(--navy-dark);
  font-family:'DM Sans',sans-serif;font-weight:600;font-size:15px;
  padding:14px 36px;border-radius:var(--radius-md);border:none;
  cursor:pointer;letter-spacing:.02em;transition:all .2s ease;
  text-decoration:none;
}
.btn-gold:hover{background:var(--gold-light);transform:translateY(-1px);box-shadow:0 6px 24px rgba(201,168,76,.3);}
.btn-navy{
  display:inline-flex;align-items:center;gap:8px;
  background:var(--navy-dark);color:var(--gold);
  font-family:'DM Sans',sans-serif;font-weight:600;font-size:15px;
  padding:14px 36px;border-radius:var(--radius-md);border:none;
  cursor:pointer;letter-spacing:.02em;transition:all .2s ease;
}
.btn-navy:hover{background:var(--navy);}

/* ── Navigation ───────────────────────────────────────── */
.nav{
  background:var(--navy-dark);
  padding:0 40px;
  display:flex;align-items:center;justify-content:space-between;
  height:68px;
  position:sticky;top:0;z-index:500;
  border-bottom:1px solid rgba(201,168,76,.12);
}
.nav-logo{
  display:flex;flex-direction:column;gap:1px;
}
.nav-logo-main{
  font-family:'Cormorant Garamond',serif;
  color:var(--gold);font-size:20px;font-weight:700;letter-spacing:.02em;line-height:1.1;
}
.nav-logo-sub{
  font-size:10px;color:rgba(201,168,76,.45);
  font-family:'DM Sans',sans-serif;font-weight:500;
  letter-spacing:.12em;text-transform:uppercase;
}
.nav-links{display:flex;align-items:center;gap:4px;}
.nav-links a{
  color:rgba(255,255,255,.6);font-size:13px;font-weight:500;
  text-decoration:none;padding:8px 14px;border-radius:var(--radius-sm);
  transition:all .18s;
}
.nav-links a:hover{color:var(--gold-light);background:rgba(255,255,255,.04);}
.nav-cta{
  background:var(--gold);color:var(--navy-dark);
  font-size:13px;font-weight:600;padding:9px 22px;
  border-radius:var(--radius-sm);border:none;cursor:pointer;
  margin-left:8px;font-family:'DM Sans',sans-serif;
  transition:all .2s;letter-spacing:.02em;
}
.nav-cta:hover{background:var(--gold-light);}

/* ── Hero ─────────────────────────────────────────────── */
.hero{
  background:linear-gradient(148deg,var(--navy-dark) 0%,var(--navy) 52%,var(--navy-light) 100%);
  padding:96px 40px 120px;
  text-align:center;
  position:relative;overflow:hidden;
}
.hero::before{
  content:'';position:absolute;inset:0;
  background:radial-gradient(ellipse 900px 500px at 50% -10%,rgba(201,168,76,.06) 0%,transparent 70%);
  pointer-events:none;
}
.hero::after{
  content:'';position:absolute;
  bottom:-1px;left:0;right:0;height:80px;
  background:linear-gradient(to bottom,transparent,var(--off-white));
  pointer-events:none;
}
.hero-badge{
  display:inline-flex;align-items:center;gap:9px;
  background:rgba(201,168,76,.09);border:1px solid rgba(201,168,76,.28);
  color:var(--gold-light);font-size:11px;font-weight:600;
  padding:7px 20px;border-radius:100px;margin-bottom:30px;
  letter-spacing:.14em;text-transform:uppercase;
}
.hero-badge-dot{
  width:6px;height:6px;border-radius:50%;background:var(--gold);
  animation:shimmer 2s ease infinite;
}
.hero h1{
  color:#fff;font-size:clamp(40px,5.5vw,70px);font-weight:700;
  line-height:1.06;margin-bottom:24px;
  max-width:840px;margin-left:auto;margin-right:auto;
}
.hero h1 .gold{color:var(--gold);}
.hero-sub{
  color:rgba(255,255,255,.58);font-size:18px;font-weight:300;
  max-width:500px;margin:0 auto 50px;line-height:1.75;
}
.trust-row{
  display:flex;justify-content:center;gap:28px;flex-wrap:wrap;
  margin-top:52px;position:relative;z-index:1;
}
.trust-item{
  color:rgba(255,255,255,.48);font-size:13px;
  display:flex;align-items:center;gap:7px;
}
.trust-check{color:var(--gold);font-size:14px;}

/* ── Upload Zone ──────────────────────────────────────── */
.upload-wrap{
  max-width:660px;margin:0 auto;position:relative;z-index:1;
}
.upload-zone{
  background:rgba(255,255,255,.04);
  border:2px dashed rgba(201,168,76,.38);
  border-radius:var(--radius-xl);
  padding:56px 36px;
  cursor:pointer;
  transition:all .25s ease;
}
.upload-zone:hover,.upload-zone.over{
  background:rgba(201,168,76,.07);
  border-color:var(--gold);
  transform:translateY(-2px);
}
.upload-icon-ring{
  width:76px;height:76px;border-radius:50%;
  background:rgba(201,168,76,.09);border:1px solid rgba(201,168,76,.22);
  display:flex;align-items:center;justify-content:center;
  margin:0 auto 22px;font-size:32px;
}
.upload-title{color:#fff;font-size:21px;font-weight:600;margin-bottom:10px;}
.upload-sub{color:rgba(255,255,255,.42);font-size:14px;margin-bottom:30px;line-height:1.65;}
.upload-formats{
  color:rgba(255,255,255,.22);font-size:11px;
  margin-top:20px;letter-spacing:.04em;
}

/* ── Analyzing State ──────────────────────────────────── */
.analyzing-box{
  max-width:500px;margin:0 auto;
  background:rgba(255,255,255,.05);
  border-radius:var(--radius-xl);
  padding:56px 36px;
  border:1px solid rgba(201,168,76,.18);
  text-align:center;
}
.spinner{
  width:48px;height:48px;
  border:3px solid rgba(201,168,76,.14);
  border-top:3px solid var(--gold);
  border-radius:50%;
  animation:spin 1s linear infinite;
  margin:0 auto 22px;
}
.analyzing-title{color:var(--gold-light);font-size:17px;font-weight:600;margin-bottom:10px;}
.analyzing-steps{color:rgba(255,255,255,.32);font-size:12px;line-height:2;margin-bottom:28px;}
.analyzing-step{display:block;animation:shimmer 2s ease infinite;}
.analyzing-step:nth-child(2){animation-delay:.4s;}
.analyzing-step:nth-child(3){animation-delay:.8s;}
.analyzing-step:nth-child(4){animation-delay:1.2s;}
.analyzing-file{
  color:rgba(255,255,255,.18);font-size:11px;
  padding-top:20px;border-top:1px solid rgba(255,255,255,.05);
  letter-spacing:.02em;
}

/* ── Error Box ────────────────────────────────────────── */
.error-box{
  max-width:500px;margin:0 auto;
  background:rgba(239,68,68,.07);
  border-radius:var(--radius-lg);
  padding:32px 36px;
  border:1px solid rgba(239,68,68,.22);
  text-align:center;
}
.error-title{color:#FCA5A5;font-size:16px;font-weight:600;margin-bottom:10px;}
.error-msg{color:rgba(252,165,165,.7);font-size:13px;margin-bottom:22px;line-height:1.6;}

/* ── Success Note ─────────────────────────────────────── */
.success-note{
  display:flex;align-items:center;justify-content:center;gap:10px;
  color:var(--gold-light);font-size:14px;font-weight:500;
  margin-top:22px;animation:fadeUp .5s ease forwards;
}

/* ── Content Sections ─────────────────────────────────── */
.section{max-width:1100px;margin:0 auto;padding:92px 40px;}
.section-label{
  color:var(--gold);font-size:11px;font-weight:700;
  letter-spacing:.18em;text-transform:uppercase;margin-bottom:14px;
}
.section-title{
  font-size:clamp(30px,4vw,48px);font-weight:700;
  color:var(--navy-dark);line-height:1.1;margin-bottom:18px;
}
.section-sub{
  color:var(--muted);font-size:17px;font-weight:300;
  line-height:1.75;max-width:540px;
}

/* ── Feature Cards ────────────────────────────────────── */
.feat-grid{
  display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
  gap:20px;margin-top:54px;
}
.feat-card{
  background:var(--white);border-radius:var(--radius-lg);
  border:1px solid var(--border);padding:32px 28px;
  transition:transform .2s ease,box-shadow .2s ease;
}
.feat-card:hover{transform:translateY(-3px);box-shadow:var(--shadow-lift);}
.feat-icon{font-size:28px;margin-bottom:16px;display:block;}
.feat-title{font-weight:600;font-size:15px;color:var(--navy);margin-bottom:9px;}
.feat-desc{color:var(--muted);font-size:13px;line-height:1.7;}

/* ── How It Works ─────────────────────────────────────── */
.how-bg{background:var(--navy);}
.how-section{max-width:1100px;margin:0 auto;padding:92px 40px;text-align:center;}
.step-grid{
  display:grid;grid-template-columns:repeat(auto-fit,minmax(230px,1fr));
  gap:44px;margin-top:56px;
}
.step-card{text-align:center;}
.step-num{
  width:58px;height:58px;border-radius:50%;
  background:rgba(201,168,76,.1);border:1px solid rgba(201,168,76,.32);
  display:flex;align-items:center;justify-content:center;
  margin:0 auto 20px;
  color:var(--gold);font-family:'Cormorant Garamond',serif;
  font-size:28px;font-weight:700;
}
.step-title{color:#fff;font-weight:600;font-size:16px;margin-bottom:11px;}
.step-desc{color:rgba(255,255,255,.48);font-size:13px;line-height:1.75;}
.report-links{
  display:flex;gap:20px;flex-wrap:wrap;justify-content:center;margin-top:52px;
}
.report-link{
  background:rgba(201,168,76,.08);border:1px solid rgba(201,168,76,.28);
  color:var(--gold-light);font-size:13px;padding:11px 24px;
  border-radius:var(--radius-md);text-decoration:none;font-weight:500;
  transition:all .2s;
}
.report-link:hover{background:rgba(201,168,76,.15);border-color:var(--gold);}

/* ── FICO Factors ─────────────────────────────────────── */
.fico-grid{
  display:grid;grid-template-columns:repeat(auto-fit,minmax(185px,1fr));
  gap:16px;margin-top:52px;
}
.fico-card{
  background:var(--white);border-radius:var(--radius-md);
  padding:26px 22px;border-top:3px solid var(--gold);
}
.fico-pct{
  font-family:'Cormorant Garamond',serif;
  font-size:42px;font-weight:700;color:var(--navy-dark);line-height:1;
}
.fico-label{font-weight:600;font-size:14px;color:var(--navy);margin:8px 0 9px;}
.fico-desc{color:var(--muted);font-size:12px;line-height:1.65;}

/* ── FAQ ──────────────────────────────────────────────── */
.faq-wrap{max-width:720px;margin:0 auto;padding:92px 40px;}
.faq-item{border-bottom:1px solid rgba(0,0,0,.08);padding:24px 0;}
.faq-item:first-child{border-top:1px solid rgba(0,0,0,.08);}
.faq-q{
  display:flex;justify-content:space-between;align-items:center;
  cursor:pointer;gap:16px;user-select:none;
}
.faq-q-text{font-weight:600;font-size:15px;color:var(--navy);line-height:1.4;}
.faq-toggle{
  color:var(--gold);font-size:24px;font-weight:300;
  flex-shrink:0;line-height:1;width:28px;text-align:center;
  transition:transform .2s;
}
.faq-a{
  color:var(--muted);font-size:14px;line-height:1.8;
  margin-top:14px;display:none;
}
.faq-a.open{display:block;animation:fadeUp .3s ease forwards;}

/* ── Final CTA Banner ─────────────────────────────────── */
.cta-banner{
  background:var(--navy);
  padding:92px 40px;text-align:center;
}
.cta-banner h2{
  color:#fff;font-size:clamp(30px,4vw,54px);
  font-weight:700;margin-bottom:22px;
  max-width:620px;margin-left:auto;margin-right:auto;
  line-height:1.08;
}
.cta-banner p{
  color:rgba(255,255,255,.52);font-size:16px;font-weight:300;
  max-width:480px;margin:0 auto 44px;line-height:1.8;
}
.cta-note{color:rgba(255,255,255,.28);font-size:12px;margin-top:16px;letter-spacing:.02em;}

/* ── Footer ───────────────────────────────────────────── */
.footer{
  background:var(--navy-dark);
  padding:56px 40px;text-align:center;
  border-top:1px solid rgba(201,168,76,.08);
}
.footer-logo{font-family:'Cormorant Garamond',serif;color:var(--gold);font-size:22px;font-weight:700;margin-bottom:6px;}
.footer-powered{color:rgba(255,255,255,.22);font-size:10px;letter-spacing:.14em;text-transform:uppercase;margin-bottom:24px;}
.footer-text{color:rgba(255,255,255,.3);font-size:12px;line-height:2;max-width:600px;margin:0 auto;}
.footer-text a{color:rgba(201,168,76,.5);text-decoration:none;}
.footer-text a:hover{color:var(--gold);}

/* ── Report Output ────────────────────────────────────── */
.report-wrap{max-width:900px;margin:0 auto;padding:52px 40px 80px;}
.report-card{
  background:var(--white);border-radius:var(--radius-xl);
  box-shadow:0 4px 48px rgba(0,0,0,.09);overflow:hidden;
}
.report-header{
  background:linear-gradient(140deg,var(--navy-dark) 0%,var(--navy) 100%);
  padding:48px 44px 40px;border-bottom:2px solid var(--gold);
}
.report-brand{font-family:'Cormorant Garamond',serif;color:var(--gold);font-size:22px;font-weight:700;}
.report-powered{color:rgba(255,255,255,.32);font-size:10px;letter-spacing:.12em;text-transform:uppercase;margin:5px 0 30px;}
.report-client{color:#fff;font-family:'Cormorant Garamond',serif;font-size:36px;font-weight:600;line-height:1.1;}
.report-date{color:rgba(255,255,255,.4);font-size:13px;margin-top:7px;}
.sec-head{
  background:var(--off-white);padding:15px 44px;
  border-bottom:1px solid rgba(0,0,0,.06);
  font-size:10px;font-weight:700;letter-spacing:.16em;
  text-transform:uppercase;color:var(--navy);
}
.sec-body{padding:34px 44px;}

/* Snapshot grid */
.snap-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(105px,1fr));gap:10px;margin-bottom:26px;}
.snap-card{background:var(--off-white);border-radius:var(--radius-sm);padding:14px 10px;text-align:center;}
.snap-val{font-family:'Cormorant Garamond',serif;font-size:24px;font-weight:700;color:var(--navy-dark);display:block;}
.snap-label{font-size:10px;color:var(--muted);font-weight:500;margin-top:4px;line-height:1.3;}

/* Score pills */
.score-row{display:flex;gap:12px;flex-wrap:wrap;margin-bottom:26px;}
.score-pill{
  background:var(--navy-dark);border-radius:var(--radius-md);
  padding:18px 24px;text-align:center;flex:1;min-width:110px;
}
.score-pill-val{font-family:'Cormorant Garamond',serif;font-size:32px;font-weight:700;color:var(--gold);display:block;line-height:1;}
.score-pill-bureau{font-size:11px;color:rgba(255,255,255,.4);margin-top:6px;letter-spacing:.06em;}

/* Rating badge */
.rating-badge{display:inline-flex;align-items:center;padding:6px 18px;border-radius:100px;font-size:12px;font-weight:700;letter-spacing:.06em;}

/* Negative items */
.neg-item{border-left:3px solid #f97316;background:#FFFCF9;border-radius:0 var(--radius-md) var(--radius-md) 0;padding:24px 22px;margin-bottom:16px;}
.neg-name{font-weight:700;color:var(--navy-dark);font-size:15px;margin-bottom:5px;}
.neg-meta{font-size:12px;color:var(--muted);margin-bottom:13px;}
.tag{display:inline-flex;align-items:center;font-size:11px;font-weight:600;padding:4px 11px;border-radius:4px;margin:0 5px 5px 0;}
.tag-warn{background:#FEF3C7;color:#92400E;}
.tag-bureau{background:#EEF2FF;color:#3730A3;}
.tag-balance{background:#FEE2E2;color:#991B1B;}
.neg-label{font-size:10px;font-weight:700;color:var(--muted);text-transform:uppercase;letter-spacing:.08em;margin:13px 0 5px;}
.neg-text{font-size:13px;color:#374151;line-height:1.75;}

/* Util bar */
.util-bar{height:10px;background:#E5E7EB;border-radius:5px;overflow:hidden;margin-bottom:5px;}
.util-fill{height:100%;border-radius:5px;transition:width 1.2s cubic-bezier(.22,.68,0,1.2);}

/* Inquiry row */
.inq-row{display:flex;justify-content:space-between;align-items:center;padding:14px 0;}
.inq-row+.inq-row{border-top:1px solid rgba(0,0,0,.05);}

/* Phase plan */
.phase-block{margin-bottom:26px;}
.phase-title{font-weight:700;color:var(--navy-dark);font-size:14px;margin-bottom:11px;display:flex;align-items:center;gap:9px;}
.phase-dot{width:9px;height:9px;border-radius:50%;flex-shrink:0;}
.phase-list{list-style:none;padding:0;}
.phase-item{font-size:13px;color:#374151;padding:7px 0 7px 22px;position:relative;line-height:1.65;}
.phase-item::before{content:'→';position:absolute;left:0;color:var(--gold-dark);font-weight:700;}
.phase-item+.phase-item{border-top:1px solid rgba(0,0,0,.04);}

/* Score potential */
.potential-box{background:linear-gradient(140deg,var(--navy-dark),var(--navy));border-radius:var(--radius-lg);padding:40px;text-align:center;}
.potential-pts{font-family:'Cormorant Garamond',serif;font-size:68px;font-weight:700;color:var(--gold);line-height:1;}
.potential-label{color:rgba(255,255,255,.45);font-size:13px;margin:12px 0 26px;}
.potential-tag{background:rgba(201,168,76,.12);border:1px solid rgba(201,168,76,.25);color:var(--gold-light);font-size:12px;padding:5px 15px;border-radius:100px;display:inline-block;margin:4px;}

/* Report CTA */
.report-cta-box{background:linear-gradient(135deg,var(--gold) 0%,var(--gold-dark) 100%);border-radius:var(--radius-lg);padding:48px;text-align:center;}
.report-cta-title{font-family:'Cormorant Garamond',serif;font-size:30px;font-weight:700;color:var(--navy-dark);margin-bottom:14px;}
.report-cta-desc{color:rgba(7,15,30,.65);font-size:15px;margin-bottom:32px;line-height:1.75;max-width:540px;margin-left:auto;margin-right:auto;}
.report-cta-services{display:flex;flex-wrap:wrap;gap:8px;justify-content:center;margin-top:26px;}
.service-tag{background:rgba(7,15,30,.08);border:1px solid rgba(7,15,30,.12);color:rgba(7,15,30,.55);font-size:11px;font-weight:600;padding:5px 14px;border-radius:100px;}
.disclaimer{color:#9CA3AF;font-size:11px;margin-top:30px;line-height:1.9;text-align:center;}

/* Another report btn */
.another-btn{
  display:block;margin:0 auto 60px;
  background:transparent;border:1px solid rgba(12,31,63,.25);
  color:var(--navy);padding:11px 30px;border-radius:var(--radius-md);
  cursor:pointer;font-size:14px;font-family:'DM Sans',sans-serif;
  transition:all .2s;
}
.another-btn:hover{border-color:var(--navy);background:rgba(12,31,63,.05);}

/* ── Mobile ───────────────────────────────────────────── */
@media(max-width:768px){
  .nav{padding:0 20px;height:60px;}
  .nav-links,.nav-cta{display:none;}
  .hero{padding:64px 20px 80px;}
  .section,.how-section,.faq-wrap{padding:64px 20px;}
  .sec-head{padding:13px 20px;}
  .sec-body{padding:24px 20px;}
  .report-header{padding:28px 20px;}
  .report-wrap{padding:32px 16px 60px;}
  .score-row{gap:8px;}
  .cta-banner{padding:64px 20px;}
  .trust-row{gap:14px;}
}
@media(max-width:480px){
  .snap-grid{grid-template-columns:repeat(3,1fr);}
  .step-grid{gap:28px;}
}
</style>
</head>
<body>

<!-- ── Navigation ── -->
<nav class="nav">
  <div class="nav-logo">
    <span class="nav-logo-main">700 Credit Club Experts</span>
    <span class="nav-logo-sub">Powered by JECI AI</span>
  </div>
  <div class="nav-links">
    <a href="https://www.700creditclubexperts.com">Home</a>
    <a href="https://www.700creditclubexperts.com/services">Services</a>
    <a href="https://www.700creditclubexperts.com/resources">Resources</a>
    <a href="https://www.700creditclubexperts.com/about">About</a>
    <button class="nav-cta" onclick="window.open('https://www.700creditclubexperts.com','_blank')">Get Started</button>
  </div>
</nav>

<!-- ── Hero ── -->
<section class="hero">
  <div class="hero-badge"><span class="hero-badge-dot"></span> JECI AI &nbsp;·&nbsp; Free Credit Audit Tool</div>
  <h1>Your Free <span class="gold">Credit Audit</span><br/>Starts Here</h1>
  <p class="hero-sub">Upload your credit report and receive a full, professional credit analysis in seconds. No cost. No obligation. No hard pull.</p>

  <!-- Upload Zone -->
  <div class="upload-wrap">
    <div id="upload-zone" class="upload-zone"
      onclick="document.getElementById('file-input').click()"
      ondragover="handleDragOver(event)"
      ondragleave="handleDragLeave(event)"
      ondrop="handleDrop(event)">
      <div class="upload-icon-ring">📄</div>
      <div class="upload-title">Upload Your Credit Report</div>
      <div class="upload-sub">Drag & drop your PDF or image file here,<br/>or click to browse your device</div>
      <button class="btn-gold" onclick="event.stopPropagation();document.getElementById('file-input').click()">
        Choose File
      </button>
      <div class="upload-formats">Supports: PDF · JPG · PNG · WEBP &nbsp;·&nbsp; Max 10MB &nbsp;·&nbsp; Privacy protected</div>
      <input id="file-input" type="file" accept=".pdf,.jpg,.jpeg,.png,.webp" style="display:none" onchange="handleFileInput(event)"/>
    </div>

    <!-- Analyzing -->
    <div id="analyzing-box" class="analyzing-box" style="display:none">
      <div class="spinner"></div>
      <div class="analyzing-title">JECI AI is analyzing your report…</div>
      <div class="analyzing-steps">
        <span class="analyzing-step">✦ &nbsp;Reading account history across all three bureaus</span>
        <span class="analyzing-step">✦ &nbsp;Identifying negative items and derogatory marks</span>
        <span class="analyzing-step">✦ &nbsp;Calculating utilization and inquiry impact</span>
        <span class="analyzing-step">✦ &nbsp;Building your personalized repair roadmap</span>
      </div>
      <div class="analyzing-file" id="file-name-display"></div>
    </div>

    <!-- Error -->
    <div id="error-box" class="error-box" style="display:none">
      <div class="error-title">Analysis Error</div>
      <div class="error-msg" id="error-msg"></div>
      <button class="btn-gold" style="background:transparent;border:1px solid rgba(239,68,68,.35);color:#FCA5A5;" onclick="resetUI()">
        Try Again
      </button>
    </div>

    <!-- Success note -->
    <div id="success-note" class="success-note" style="display:none">
      <span>✓</span> Analysis complete — your full report is below
    </div>
  </div>

  <div class="trust-row">
    <div class="trust-item"><span class="trust-check">✓</span> 100% Free</div>
    <div class="trust-item"><span class="trust-check">✓</span> No Hard Pull</div>
    <div class="trust-item"><span class="trust-check">✓</span> Privacy Protected</div>
    <div class="trust-item"><span class="trust-check">✓</span> Results in Seconds</div>
    <div class="trust-item"><span class="trust-check">✓</span> All 3 Bureaus Covered</div>
  </div>
</section>

<!-- ── Report Output ── -->
<div id="report-output"></div>
<div id="another-wrap" style="display:none;text-align:center;padding-top:8px;">
  <button class="another-btn" onclick="resetUI()">↑ Analyze Another Report</button>
</div>

<!-- ── What We Analyze ── -->
<section style="background:var(--off-white);">
  <div class="section">
    <div class="section-label">What We Cover</div>
    <h2 class="section-title">Everything Your Credit Score<br/>Depends On</h2>
    <p class="section-sub">JECI AI doesn't just hand you a number — it breaks down every factor affecting your score and tells you exactly what to do about it.</p>
    <div class="feat-grid">
      <div class="feat-card"><span class="feat-icon">📊</span><div class="feat-title">Score Analysis</div><div class="feat-desc">Bureau-level score breakdown across Experian, Equifax, and TransUnion with full contextual insights for each.</div></div>
      <div class="feat-card"><span class="feat-icon">⚠️</span><div class="feat-title">Negative Item Audit</div><div class="feat-desc">Every derogatory mark identified and explained — collections, charge-offs, late payments, repossessions, and more.</div></div>
      <div class="feat-card"><span class="feat-icon">💳</span><div class="feat-title">Utilization Review</div><div class="feat-desc">Card-by-card utilization breakdown with exact paydown targets to maximize the 30% of your score it controls.</div></div>
      <div class="feat-card"><span class="feat-icon">🔍</span><div class="feat-title">Inquiry Scan</div><div class="feat-desc">All hard inquiries listed with 24-month fall-off timelines and dispute eligibility assessed for each one.</div></div>
      <div class="feat-card"><span class="feat-icon">📅</span><div class="feat-title">Account Age Report</div><div class="feat-desc">Credit history length and mix evaluated — the 15% of your FICO score most people completely overlook.</div></div>
      <div class="feat-card"><span class="feat-icon">🗺️</span><div class="feat-title">Repair Roadmap</div><div class="feat-desc">A personalized three-phase action plan with specific, prioritized steps tailored to your exact credit profile.</div></div>
    </div>
  </div>
</section>

<!-- ── How It Works ── -->
<section class="how-bg">
  <div class="how-section">
    <div class="section-label" style="color:var(--gold);">How It Works</div>
    <h2 class="section-title" style="color:#fff;">Three Steps to Your<br/>Professional Credit Audit</h2>
    <div class="step-grid">
      <div class="step-card">
        <div class="step-num">1</div>
        <div class="step-title">Pull Your Free Report</div>
        <div class="step-desc">Get your credit report from AnnualCreditReport.com or Credit Hero Score at no cost. No credit card needed.</div>
      </div>
      <div class="step-card">
        <div class="step-num">2</div>
        <div class="step-title">Upload & Analyze</div>
        <div class="step-desc">JECI AI reads every account, balance, inquiry, and derogatory mark across all three credit bureaus simultaneously.</div>
      </div>
      <div class="step-card">
        <div class="step-num">3</div>
        <div class="step-title">Get Your Action Plan</div>
        <div class="step-desc">Receive a full professional audit report with a prioritized three-phase credit improvement strategy built for your profile.</div>
      </div>
    </div>
    <div class="report-links">
      <a class="report-link" href="https://www.annualcreditreport.com" target="_blank" rel="noopener">Get Free Report → AnnualCreditReport.com</a>
      <a class="report-link" href="https://www.credithero.com" target="_blank" rel="noopener">Get Free Report → Credit Hero Score</a>
    </div>
  </div>
</section>

<!-- ── FICO Factors ── -->
<section style="background:var(--white);">
  <div class="section">
    <div class="section-label">Credit Education</div>
    <h2 class="section-title">Know What Moves Your Score</h2>
    <p class="section-sub">Your FICO score is calculated from five weighted categories. Most people only focus on one — here's the full picture.</p>
    <div class="fico-grid">
      <div class="fico-card"><div class="fico-pct">35%</div><div class="fico-label">Payment History</div><div class="fico-desc">The single biggest factor. One missed payment can drop your score by 80–100 points overnight.</div></div>
      <div class="fico-card"><div class="fico-pct">30%</div><div class="fico-label">Credit Utilization</div><div class="fico-desc">Keep all revolving balances under 10% of your credit limits for the highest possible score impact.</div></div>
      <div class="fico-card"><div class="fico-pct">15%</div><div class="fico-label">Credit Age</div><div class="fico-desc">Longer history signals reliability. Never close your oldest account — even if you no longer use it.</div></div>
      <div class="fico-card"><div class="fico-pct">10%</div><div class="fico-label">Credit Mix</div><div class="fico-desc">Having both revolving credit cards and installment loans signals well-rounded credit management.</div></div>
      <div class="fico-card"><div class="fico-pct">10%</div><div class="fico-label">New Credit</div><div class="fico-desc">Multiple hard inquiries in a short window signal risk to lenders. Space applications at least 6 months apart.</div></div>
    </div>
  </div>
</section>

<!-- ── FAQ ── -->
<section style="background:var(--off-white);">
  <div class="faq-wrap">
    <div class="section-label">Common Questions</div>
    <h2 class="section-title">Frequently Asked Questions</h2>
    <div style="margin-top:44px;">
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(0)">
          <span class="faq-q-text">Is this credit audit really free?</span>
          <span class="faq-toggle" id="ft-0">+</span>
        </div>
        <div class="faq-a" id="fa-0">Yes. Your Free Credit Audit from 700 Credit Club Experts is completely free. No credit card, no obligation, and no hidden fees of any kind — now or ever.</div>
      </div>
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(1)">
          <span class="faq-q-text">What type of credit report can I upload?</span>
          <span class="faq-toggle" id="ft-1">+</span>
        </div>
        <div class="faq-a" id="fa-1">Any credit report in PDF or image format — from AnnualCreditReport.com, Credit Hero Score, myFICO, Experian, Equifax, TransUnion, or any bureau-generated report. The more complete the report, the deeper the analysis.</div>
      </div>
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(2)">
          <span class="faq-q-text">Is my personal information secure?</span>
          <span class="faq-toggle" id="ft-2">+</span>
        </div>
        <div class="faq-a" id="fa-2">Yes. Your report is analyzed in real time using privacy-first protocols. All sensitive identifiers — Social Security numbers, full account numbers, addresses — are automatically masked in the output. Your uploaded file is not stored on our servers.</div>
      </div>
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(3)">
          <span class="faq-q-text">Will this hurt my credit score?</span>
          <span class="faq-toggle" id="ft-3">+</span>
        </div>
        <div class="faq-a" id="fa-3">No. Uploading and analyzing your existing credit report creates zero impact on your credit score. Only formal credit applications — for loans, credit cards, or financing — trigger hard inquiries that can temporarily affect your score.</div>
      </div>
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(4)">
          <span class="faq-q-text">What happens after I receive my audit?</span>
          <span class="faq-toggle" id="ft-4">+</span>
        </div>
        <div class="faq-a" id="fa-4">You'll receive a full three-phase improvement roadmap you can act on yourself. Or, you can choose to have the 700 Credit Club team manage the entire repair process — disputes, bureau communications, and credit building — on your behalf.</div>
      </div>
      <div class="faq-item">
        <div class="faq-q" onclick="toggleFaq(5)">
          <span class="faq-q-text">How accurate is the JECI AI analysis?</span>
          <span class="faq-toggle" id="ft-5">+</span>
        </div>
        <div class="faq-a" id="fa-5">JECI AI reads your uploaded report directly and extracts account-level data with high accuracy. All output is for educational purposes — not legal or financial advice. We always recommend verifying all report data directly with the credit bureaus.</div>
      </div>
    </div>
  </div>
</section>

<!-- ── Final CTA Banner ── -->
<section class="cta-banner">
  <div class="hero-badge" style="margin-bottom:28px;">Ready to Transform Your Credit?</div>
  <h2>Let 700 Credit Club Handle<br/>The Heavy Lifting</h2>
  <p>Our certified credit specialists manage disputes, bureau communications, creditor validation, and credit building — so you can focus on your financial future.</p>
  <button class="btn-gold" style="font-size:16px;padding:17px 52px;" onclick="window.open('https://www.700creditclubexperts.com','_blank')">
    Start My Credit Repair →
  </button>
  <div class="cta-note">A certified specialist will contact you within 24 hours</div>
</section>

<!-- ── Footer ── -->
<footer class="footer">
  <div class="footer-logo">700 Credit Club Experts</div>
  <div class="footer-powered">Powered by JECI AI</div>
  <div class="footer-text">
    This tool is for educational purposes only and does not constitute legal, tax, or financial advice.<br/>
    700 Credit Club Experts does not guarantee credit score increases or the removal of any negative items.<br/>
    Credit results vary based on individual circumstances and the accuracy of credit bureau reporting.<br/><br/>
    <a href="https://www.700creditclubexperts.com" target="_blank">700creditclubexperts.com</a>
    &nbsp;·&nbsp; © 2025 700 Credit Club Experts. All rights reserved.
  </div>
</footer>

<!-- ════════════════════════════════════════════════════
     JECI AI ENGINE
     ════════════════════════════════════════════════════ -->
<script>
/* ── Anthropic API Key ─────────────────────────────────────────────────
   IMPORTANT: Replace 'YOUR_ANTHROPIC_API_KEY' with your actual key.
   For production, proxy this call through your backend to protect the key.
   Example backend route: POST /api/analyze-credit-report
   ───────────────────────────────────────────────────────────────────── */
const ANTHROPIC_API_KEY = 'YOUR_ANTHROPIC_API_KEY';
const ANTHROPIC_API_URL = 'https://api.anthropic.com/v1/messages';

/* ── System Prompt ─────────────────────────────────────────────────── */
const SYSTEM_PROMPT = `You are a Senior Credit Analyst at 700 Credit Club Experts, powered by JECI AI.
Analyze the uploaded credit report and return ONLY a valid JSON object — no markdown, no code fences, no preamble, no postamble.

Use this exact structure:

{
  "clientName": "<First initial + Last initial only — e.g. J.D. Never use full names>",
  "reportDate": "<Date found in report, or today's date>",
  "scores": {
    "experian": <number or null>,
    "equifax": <number or null>,
    "transunion": <number or null>
  },
  "snapshot": {
    "totalAccounts": <number>,
    "openAccounts": <number>,
    "closedAccounts": <number>,
    "derogatoryAccounts": <number>,
    "collections": <number>,
    "chargeOffs": <number>,
    "latePayments": <number>,
    "hardInquiries": <number>,
    "utilizationPercent": <number>
  },
  "healthRating": "<Excellent | Good | Fair | Needs Improvement | High Risk>",
  "healthExplanation": "<2-3 sentences explaining the rating in plain language>",
  "negativeItems": [
    {
      "accountName": "<Name with account number masked — show last 4 digits only if present>",
      "accountType": "<Credit Card | Auto Loan | Collection | Medical | etc.>",
      "balance": "<formatted dollar amount>",
      "status": "<Collection | Charge-Off | Late Payment | Repossession | etc.>",
      "dateOpened": "<date>",
      "lastActivity": "<date>",
      "bureaus": ["<Experian and/or Equifax and/or TransUnion>"],
      "impact": "<How this specific item impacts the credit score — 2 sentences>",
      "lenderView": "<How lenders view this type of derogatory mark — 2 sentences>",
      "disputability": "<Whether the item appears potentially disputable or verifiable — 2 sentences. Do not give legal advice.>"
    }
  ],
  "utilization": {
    "currentPercent": <number>,
    "explanation": "<Explain utilization impact and the 30% FICO weight in plain language>",
    "cards": [
      {
        "name": "<card/account name>",
        "limit": <number — credit limit>,
        "balance": <number — current balance>,
        "utilization": <number — percentage>
      }
    ]
  },
  "inquiries": [
    {
      "creditor": "<creditor name>",
      "date": "<date>",
      "bureau": "<bureau name>",
      "disputable": <true | false>
    }
  ],
  "creditAge": {
    "oldestAccount": "<description of oldest account and approximate age>",
    "averageAge": "<e.g. 4 years 3 months>",
    "mix": ["<account type 1>", "<account type 2>", "<etc.>"],
    "explanation": "<Explain how credit age and mix affect the score — 2-3 sentences>"
  },
  "improvementPlan": {
    "phase1": [
      "<Specific action step 1 for cleaning up negative items>",
      "<Specific action step 2>",
      "<Specific action step 3>",
      "<Specific action step 4>"
    ],
    "phase2": [
      "<Specific action step 1 for lowering utilization>",
      "<Specific action step 2>",
      "<Specific action step 3>"
    ],
    "phase3": [
      "<Specific action step 1 for building positive credit>",
      "<Specific action step 2>",
      "<Specific action step 3>"
    ]
  },
  "scorePotential": {
    "low": <conservative point estimate>,
    "high": <optimistic point estimate>,
    "factors": [
      "<Key factor 1 that could improve the score>",
      "<Key factor 2>",
      "<Key factor 3>",
      "<Key factor 4>"
    ]
  }
}

CRITICAL PRIVACY RULES — ALWAYS ENFORCE:
- Names: initials only (e.g. J.D.) — NEVER output full names
- Account numbers: masked — show last 4 digits max
- Social Security Numbers: NEVER include
- Phone numbers, email addresses, full physical addresses: NEVER include
- This report is EDUCATIONAL ONLY — never provide legal advice, never guarantee score increases or item removals`;

/* ── State ─────────────────────────────────────────────────────────── */
let openFaqIdx = null;

/* ── Helpers ───────────────────────────────────────────────────────── */
function $(id){ return document.getElementById(id); }
function show(id){ $(id).style.display = ''; }
function hide(id){ $(id).style.display = 'none'; }

function toggleFaq(i) {
  const a = $('fa-' + i), t = $('ft-' + i);
  if (openFaqIdx !== null && openFaqIdx !== i) {
    $('fa-' + openFaqIdx).classList.remove('open');
    $('fa-' + openFaqIdx).style.display = 'none';
    $('ft-' + openFaqIdx).textContent = '+';
  }
  const isOpen = a.style.display !== 'none';
  if (isOpen) {
    a.style.display = 'none';
    a.classList.remove('open');
    t.textContent = '+';
    openFaqIdx = null;
  } else {
    a.style.display = 'block';
    a.classList.add('open');
    t.textContent = '−';
    openFaqIdx = i;
  }
}

function handleDragOver(e) {
  e.preventDefault();
  $('upload-zone').classList.add('over');
}
function handleDragLeave() {
  $('upload-zone').classList.remove('over');
}
function handleDrop(e) {
  e.preventDefault();
  $('upload-zone').classList.remove('over');
  const f = e.dataTransfer.files[0];
  if (f) processFile(f);
}
function handleFileInput(e) {
  const f = e.target.files[0];
  if (f) processFile(f);
}

function resetUI() {
  hide('analyzing-box');
  hide('error-box');
  hide('success-note');
  hide('another-wrap');
  show('upload-zone');
  $('report-output').innerHTML = '';
  $('file-input').value = '';
}

function toBase64(file) {
  return new Promise((res, rej) => {
    const r = new FileReader();
    r.onload = () => res(r.result.split(',')[1]);
    r.onerror = rej;
    r.readAsDataURL(file);
  });
}

/* ── Main Analysis Function ────────────────────────────────────────── */
async function processFile(file) {
  /* Show analyzing state */
  hide('upload-zone');
  hide('error-box');
  hide('success-note');
  $('report-output').innerHTML = '';
  hide('another-wrap');
  show('analyzing-box');
  $('file-name-display').textContent = '📁 ' + file.name;

  try {
    const base64 = await toBase64(file);
    const isPdf = file.type === 'application/pdf';
    const mediaType = isPdf ? 'application/pdf' : (file.type || 'image/jpeg');

    const contentBlock = isPdf
      ? { type: 'document', source: { type: 'base64', media_type: 'application/pdf', data: base64 } }
      : { type: 'image', source: { type: 'base64', media_type: mediaType, data: base64 } };

    /* ── NOTE FOR PRODUCTION DEPLOYMENT ──────────────────────────────
       Replace this direct API call with a call to your own backend:
         const res = await fetch('/api/analyze-credit-report', {
           method: 'POST',
           headers: { 'Content-Type': 'application/json' },
           body: JSON.stringify({ base64, mediaType, isPdf })
         });
       Your backend then makes the Anthropic call with the stored API key.
       This protects your key from client-side exposure.
       ─────────────────────────────────────────────────────────────── */
    const res = await fetch(ANTHROPIC_API_URL, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'x-api-key': ANTHROPIC_API_KEY,
        'anthropic-version': '2023-06-01',
        'anthropic-dangerous-direct-browser-access': 'true'
      },
      body: JSON.stringify({
        model: 'claude-sonnet-4-20250514',
        max_tokens: 4096,
        system: SYSTEM_PROMPT,
        messages: [{
          role: 'user',
          content: [
            contentBlock,
            { type: 'text', text: 'Analyze this credit report and return the structured JSON audit per your instructions. Be thorough and specific — read every account, balance, and negative item visible in the report.' }
          ]
        }]
      })
    });

    if (!res.ok) {
      const errData = await res.json().catch(() => ({}));
      throw new Error(errData.error?.message || 'API request failed with status ' + res.status);
    }

    const data = await res.json();
    const raw = (data.content || []).map(b => b.text || '').join('');
    const clean = raw.replace(/```json\n?|```\n?/g, '').trim();
    const report = JSON.parse(clean);

    /* Success */
    hide('analyzing-box');
    $('success-note').style.display = 'flex';
    renderReport(report);
    show('another-wrap');
    setTimeout(() => $('report-output').scrollIntoView({ behavior: 'smooth', block: 'start' }), 250);

  } catch (err) {
    hide('analyzing-box');
    show('error-box');
    $('error-msg').textContent = err.message || 'Analysis failed. Please check your file and try again.';
    console.error('[JECI AI Error]', err);
  }
}

/* ── Utility Helpers ───────────────────────────────────────────────── */
function utilColor(pct) {
  if (pct > 50) return '#ef4444';
  if (pct > 30) return '#f97316';
  if (pct > 10) return '#f59e0b';
  return '#22c55e';
}

function ratingStyle(rating) {
  const map = {
    'Excellent':          ['#dcfce7','#166534'],
    'Good':               ['#d1fae5','#065f46'],
    'Fair':               ['#fef9c3','#713f12'],
    'Needs Improvement':  ['#ffedd5','#7c2d12'],
    'High Risk':          ['#fee2e2','#7f1d1d'],
  };
  const [bg, color] = map[rating] || ['#f3f4f6','#374151'];
  return `background:${bg};color:${color};`;
}

function mkTag(cls, text) {
  return `<span class="tag ${cls}">${text}</span>`;
}

/* ── Report Renderer ───────────────────────────────────────────────── */
function renderReport(r) {
  /* Scores */
  const scoresHtml = Object.entries(r.scores || {})
    .filter(([,v]) => v != null)
    .map(([bureau, score]) => `
      <div class="score-pill">
        <span class="score-pill-val">${score}</span>
        <div class="score-pill-bureau">${bureau.charAt(0).toUpperCase() + bureau.slice(1)}</div>
      </div>`).join('');

  /* Snapshot cards */
  const snapItems = [
    ['Total Accounts',  r.snapshot?.totalAccounts],
    ['Open Accounts',   r.snapshot?.openAccounts],
    ['Closed Accounts', r.snapshot?.closedAccounts],
    ['Derogatory',      r.snapshot?.derogatoryAccounts],
    ['Collections',     r.snapshot?.collections],
    ['Charge-Offs',     r.snapshot?.chargeOffs],
    ['Late Payments',   r.snapshot?.latePayments],
    ['Hard Inquiries',  r.snapshot?.hardInquiries],
    ['Utilization',     r.snapshot?.utilizationPercent != null ? r.snapshot.utilizationPercent + '%' : '—'],
  ];
  const snapHtml = snapItems.map(([label, val]) =>
    `<div class="snap-card">
      <span class="snap-val">${val ?? '—'}</span>
      <div class="snap-label">${label}</div>
    </div>`
  ).join('');

  /* Negative items */
  const negHtml = (r.negativeItems || []).map(item => `
    <div class="neg-item">
      <div class="neg-name">${item.accountName}</div>
      <div class="neg-meta">
        ${item.accountType}
        ${item.dateOpened ? ' &nbsp;·&nbsp; Opened: ' + item.dateOpened : ''}
        ${item.lastActivity ? ' &nbsp;·&nbsp; Last Activity: ' + item.lastActivity : ''}
      </div>
      <div>
        ${mkTag('tag-warn', item.status || 'Derogatory')}
        ${(item.bureaus || []).map(b => mkTag('tag-bureau', b)).join('')}
        ${item.balance ? mkTag('tag-balance', 'Balance: ' + item.balance) : ''}
      </div>
      <div class="neg-label">Impact on Score</div>
      <div class="neg-text">${item.impact || ''}</div>
      <div class="neg-label">How Lenders View This</div>
      <div class="neg-text">${item.lenderView || ''}</div>
      <div class="neg-label">Dispute Potential</div>
      <div class="neg-text">${item.disputability || ''}</div>
    </div>`).join('');

  /* Utilization */
  const utilPct = r.utilization?.currentPercent || 0;
  const utilCardsHtml = (r.utilization?.cards || []).map(c => {
    const upct = c.utilization || 0;
    return `
      <div style="margin-bottom:20px;">
        <div style="display:flex;justify-content:space-between;margin-bottom:7px;font-size:13px;flex-wrap:wrap;gap:4px;">
          <span style="font-weight:600;color:var(--navy);">${c.name}</span>
          <span style="color:var(--muted);">$${(c.balance||0).toLocaleString()} / $${(c.limit||0).toLocaleString()}</span>
          <span style="font-weight:700;color:${utilColor(upct)};">${upct}%</span>
        </div>
        <div class="util-bar">
          <div class="util-fill" style="width:${Math.min(upct,100)}%;background:${utilColor(upct)};"></div>
        </div>
      </div>`;
  }).join('');

  /* Inquiries */
  const inqHtml = (r.inquiries || []).map(inq => `
    <div class="inq-row">
      <div>
        <div style="font-weight:600;font-size:14px;color:var(--navy);">${inq.creditor}</div>
        <div style="font-size:12px;color:#9CA3AF;">${inq.date || ''}${inq.bureau ? ' &nbsp;·&nbsp; ' + inq.bureau : ''}</div>
      </div>
      <span class="tag" style="${inq.disputable ? 'background:#dcfce7;color:#166534;' : 'background:#F3F4F6;color:#6B7280;'}">
        ${inq.disputable ? 'Potentially Disputable' : 'Standard'}
      </span>
    </div>`).join('');

  /* Credit age */
  const ageHtml = r.creditAge ? `
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:20px;">
      <div class="snap-card">
        <span class="snap-val">${r.creditAge.averageAge || '—'}</span>
        <div class="snap-label">Average Account Age</div>
      </div>
      <div class="snap-card">
        <span class="snap-val" style="font-size:15px;line-height:1.2;">${r.creditAge.oldestAccount || '—'}</span>
        <div class="snap-label">Oldest Account</div>
      </div>
    </div>
    <div style="margin-bottom:16px;">${(r.creditAge.mix || []).map(m => mkTag('tag-bureau', m)).join('')}</div>
    <p style="color:#374151;font-size:13px;line-height:1.8;">${r.creditAge.explanation || ''}</p>
  ` : '';

  /* Improvement plan */
  const phases = [
    { label: 'Phase 1 — Clean Up Negative Items',     color: '#ef4444', items: r.improvementPlan?.phase1 || [] },
    { label: 'Phase 2 — Lower Credit Utilization',    color: '#f59e0b', items: r.improvementPlan?.phase2 || [] },
    { label: 'Phase 3 — Strengthen Your Credit Profile', color: '#22c55e', items: r.improvementPlan?.phase3 || [] },
  ];
  const planHtml = phases.map(p => `
    <div class="phase-block">
      <div class="phase-title">
        <span class="phase-dot" style="background:${p.color};"></span>
        ${p.label}
      </div>
      <ul class="phase-list">
        ${p.items.map(item => `<li class="phase-item">${item}</li>`).join('')}
      </ul>
    </div>`).join('');

  /* Score potential */
  const sp = r.scorePotential || {};
  const factorTags = (sp.factors || []).map(f => `<span class="potential-tag">${f}</span>`).join('');

  /* Services list */
  const services = ['Professional Credit Disputes','Bureau Communications','Creditor Validation','Credit Building Strategies','Ongoing Score Monitoring'];
  const serviceTags = services.map(s => `<span class="service-tag">${s}</span>`).join('');

  /* ── Assemble Report HTML ── */
  $('report-output').innerHTML = `
    <div class="report-wrap fade-up">
      <div class="report-card">

        <!-- Header -->
        <div class="report-header">
          <div class="report-brand">700 Credit Club Experts</div>
          <div class="report-powered">Professional Credit Audit Report &nbsp;·&nbsp; Powered by JECI AI</div>
          <div class="report-client">Prepared for: ${r.clientName || 'Client'}</div>
          <div class="report-date">Report Date: ${r.reportDate || new Date().toLocaleDateString('en-US',{year:'numeric',month:'long',day:'numeric'})}</div>
        </div>

        <!-- S1: Snapshot -->
        <div class="sec-head">Section 1 &nbsp;—&nbsp; Credit Profile Snapshot</div>
        <div class="sec-body">
          ${scoresHtml ? `<div class="score-row">${scoresHtml}</div>` : '<p style="color:var(--muted);font-size:14px;margin-bottom:20px;">Score data not detected in uploaded report. Please upload a report that includes bureau scores.</p>'}
          <div class="snap-grid">${snapHtml}</div>
          <div style="display:flex;align-items:center;gap:13px;flex-wrap:wrap;margin-top:4px;">
            <span style="font-weight:600;color:var(--navy);">Credit Health Rating:</span>
            <span class="rating-badge" style="${ratingStyle(r.healthRating)}">${r.healthRating || 'Not Rated'}</span>
          </div>
          <p style="color:#374151;font-size:13px;line-height:1.8;margin-top:14px;">${r.healthExplanation || ''}</p>
        </div>

        ${negHtml ? `
        <!-- S2: Negative Items -->
        <div class="sec-head">Section 2 &nbsp;—&nbsp; Negative Item Breakdown</div>
        <div class="sec-body">${negHtml}</div>
        ` : ''}

        ${r.utilization ? `
        <!-- S3: Utilization -->
        <div class="sec-head">Section 3 &nbsp;—&nbsp; Credit Utilization Analysis</div>
        <div class="sec-body">
          <div style="display:flex;align-items:center;gap:18px;margin-bottom:24px;flex-wrap:wrap;">
            <div style="flex:1;min-width:220px;">
              <div class="util-bar" style="height:14px;">
                <div class="util-fill" style="width:${Math.min(utilPct,100)}%;background:${utilColor(utilPct)};height:100%;"></div>
              </div>
              <div style="display:flex;justify-content:space-between;font-size:11px;color:#9CA3AF;margin-top:5px;">
                <span>0%</span><span style="color:${utilColor(10)};">Ideal: ≤10%</span><span>100%</span>
              </div>
            </div>
            <div style="background:var(--navy-dark);color:var(--gold);font-weight:700;font-size:28px;padding:14px 24px;border-radius:var(--radius-md);min-width:90px;text-align:center;font-family:'Cormorant Garamond',serif;">
              ${utilPct}%
            </div>
          </div>
          <p style="color:#374151;font-size:13px;line-height:1.8;margin-bottom:26px;">${r.utilization.explanation || ''}</p>
          ${utilCardsHtml}
        </div>
        ` : ''}

        ${r.inquiries?.length ? `
        <!-- S4: Inquiries -->
        <div class="sec-head">Section 4 &nbsp;—&nbsp; Hard Inquiry Review</div>
        <div class="sec-body">
          <p style="color:#374151;font-size:13px;line-height:1.8;margin-bottom:24px;">
            Hard inquiries typically reduce your score by 2–5 points each and remain on your credit report for 24 months before falling off naturally. In some cases, unauthorized or erroneous inquiries may be disputable.
          </p>
          ${inqHtml}
        </div>
        ` : ''}

        ${r.creditAge ? `
        <!-- S5: Credit Age -->
        <div class="sec-head">Section 5 &nbsp;—&nbsp; Credit Age & Mix</div>
        <div class="sec-body">${ageHtml}</div>
        ` : ''}

        ${r.improvementPlan ? `
        <!-- S6: Improvement Plan -->
        <div class="sec-head">Section 6 &nbsp;—&nbsp; Step-by-Step Credit Improvement Plan</div>
        <div class="sec-body">${planHtml}</div>
        ` : ''}

        ${sp.low ? `
        <!-- S7: Score Potential -->
        <div class="sec-head">Section 7 &nbsp;—&nbsp; Estimated Score Improvement Potential</div>
        <div class="sec-body">
          <div class="potential-box">
            <div class="potential-pts">+${sp.low}–${sp.high}</div>
            <div class="potential-label">Estimated Point Range &nbsp;·&nbsp; Individual results will vary</div>
            <div>${factorTags}</div>
          </div>
          <p style="color:var(--muted);font-size:12px;line-height:1.8;margin-top:18px;">
            This estimate is based on the specific negative items, utilization levels, and profile characteristics identified in your report.
            Actual results depend on successful dispute outcomes, payment behavior, and credit bureau processing timelines.
          </p>
        </div>
        ` : ''}

        <!-- S8: CTA -->
        <div class="sec-head">Section 8 &nbsp;—&nbsp; Let the Experts Handle This For You</div>
        <div class="sec-body">
          <div class="report-cta-box">
            <div class="report-cta-title">Start My Credit Repair With 700 Credit Club</div>
            <div class="report-cta-desc">
              You can follow this plan yourself using our free resources and guides — or let our certified credit specialists take over. We handle every step: disputes, bureau communications, creditor validation, and credit building strategies on your behalf.
            </div>
            <button class="btn-navy" style="font-size:15px;padding:16px 44px;" onclick="window.open('https://www.700creditclubexperts.com','_blank')">
              Get Started With 700 Credit Club →
            </button>
            <div class="report-cta-services">${serviceTags}</div>
          </div>
          <div class="disclaimer">
            ⚠️ This report is for educational purposes only and does not constitute legal, tax, or financial advice.<br/>
            700 Credit Club Experts does not guarantee credit score increases or the removal of any negative items.<br/>
            Credit results vary based on individual circumstances and the accuracy of credit bureau data.
          </div>
        </div>

      </div><!-- /report-card -->
    </div><!-- /report-wrap -->
  `;
}
</script>
</body>
</html>